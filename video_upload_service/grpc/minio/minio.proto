syntax = "proto3";

option go_package = "/minio";

package helloworld;

service FileService {
  rpc UploadFile(FileChunk) returns (UploadStatus);
}

message FileChunk {
  bytes data = 1;
  string fileName = 2;
  int64 chunkID = 3;
  bool done = 4;
}

message UploadStatus {
  bool success = 1;
}
